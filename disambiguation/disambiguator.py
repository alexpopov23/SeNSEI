
class Disambiguator():
    """
    Neural architecture that embeds a context, then calculates (structured) logits for classification.
    Configurable for different tasks: WSD, FrameID, NER, POS tagging, etc.
    """

    def __init__(self):
        return

    def __call__(self):
        """
        Get predictions per token in an input sequence
        :return:
        """
    def train(self):
        """
        Train the classifier
        :return:
        """

    def accuracy(self):
        """
        Calculate accuracy over a batch of sequences
        :return:
        """